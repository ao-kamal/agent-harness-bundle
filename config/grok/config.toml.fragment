# Merged by install-grok.ps1. Existing keys in ~/.grok/config.toml win.
# Do not add [skills].paths that point at a second copy of the payload.

[memory]
enabled = true

[compat.claude]
skills = true
rules = true
agents = true
mcps = true
hooks = true
