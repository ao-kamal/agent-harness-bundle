[automount]
# fstab entries bind onto /mnt/c paths; init processes fstab before /mnt/c
# is ready, so we disable boot-time fstab and let the [boot] hook below
# run mount -a once /mnt/c is up.
mountFsTab = false

[boot]
command = /usr/local/sbin/mount-fast-data.sh
